graphOfRhoHat <- function(sampleSize = 100,
                          tPar = 0.2,
                          tParCount = 5,
                          sigma = 2,
                          mean = 0,
                          lag = 0){

  testUtilGraphOfRhoHat(sampleSize = sampleSize,
                        tPar = tPar,
                        tParCount = tParCount,
                        sigma = sigma,
                        mean = mean,
                        lag = lag)
}
