createNoiseScatterPlot <- function(sampleSize = 1000,
                                   mean = 0,
                                   sigma = 2,
                                   replicationSize = 1000){

  testUtilCreateNoiseScatterPlot(sampleSize = sampleSize,
                                 mean = mean,
                                 sigma = sigma,
                                 replicationSize = replicationSize)
}
