createDiagonalPlot <- function(sampleSize = 100,
                               tParCount = 5,
                               mean = 0,
                               sigma = 2){

  testUtilCreateDiagonalPlot(sampleSize = sampleSize,
                             tParCount = tParCount,
                             mean = mean,
                             sigma = sigma)
}
