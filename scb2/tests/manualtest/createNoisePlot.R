createNoisePlot <- function(sampleSize = 100,
                            mean = 0,
                            sigma = 2){

  testUtilCreateNoisePlot(sampleSize = sampleSize,
                          mean = mean,
                          sigma = sigma)
}
