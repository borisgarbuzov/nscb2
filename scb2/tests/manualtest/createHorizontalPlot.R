createHorizontalPlot <- function(sampleSize = 100,
                                 tParCount = 5,
                                 mean = 0,
                                 sigma = 2,
                                 tPar = 0.2){

  testUtilCreateHorizontalPlot(sampleSize = sampleSize,
                               tParCount = tParCount,
                               mean = mean,
                               sigma = sigma,
                               tPar = tPar)
}
