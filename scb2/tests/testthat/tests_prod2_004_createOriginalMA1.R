test_that(desc = "Testing createOriginal for length",
		  code = {
		    testUtilCreateOriginalMA1(size = 10,
		                              psi = 0.5,
		                              sigma = 1,
		                              mean = 10)

})
