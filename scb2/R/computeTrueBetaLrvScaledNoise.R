#the function is used to calculate true LRV in the experiment with scaled noise.
#The input sd (sigma).
#The output is a trueLrv.

#called by computeTrueLrv


computeTrueBetaLrvScaledNoise <- function(){
  trueLrv <- 1
  return(trueLrv)
}
